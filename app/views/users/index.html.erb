<%= render 'layouts/page_title', title: "Available Open Hours" %>

<!-- <div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%#= will_paginate %>
  </div>
</div> -->

<% @users.each do |u| %>
    <div class="col-md-3">
      <%#= gravatar_for u, size: 200 %>
      <%= link_to image_tag(u.picture.url, size:"200", class: "gravatar") if u.picture? %>
      <h5><%= link_to "#{u.first_name} #{u.last_name}", user_path(u) %></h5>
    </div>
<% end %>

<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= will_paginate %>
  </div>
</div>
