<%= render 'layouts/page_title', title: "Available Open Hours" %>

<!-- <div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%#= will_paginate %>
  </div>
</div> -->

<% @users.each do |u| %>
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <%#= link_to gravatar_for(u, size: 200), user_path(u) %>
      <h5><%= u.first_name %> <%= u.last_name %></h5>
    </div>
  </div>
<% end %>
